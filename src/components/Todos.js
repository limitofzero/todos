import React, { Component } from "react";
// import "../less/todo.less"

export default class Todos extends Component {
    render(){
        return (
            <div className="todo">
            </div>
        );
    };
}